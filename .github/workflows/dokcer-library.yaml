name: Build, Scan, and Push Docker Images (Reusable)

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main
    paths:
      - "docker-config-*.yaml"
  workflow_dispatch:

jobs:
  build-and-push:
    uses: muna1991/Action-Demo/.github/workflows/working-dir-shell.yaml@main
    with:
      # if you pass inputs to the called workflow
    secrets: inherit
    environment:
      name: Release
