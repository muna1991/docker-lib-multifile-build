name: cicd-workflow with slack integration
on:
  push:


jobs:
  notify:
    runs-on: self-hosted
    steps:
      - name: Send Slack Message
        uses: ./.github/actions/simple-slack-action
        with:
          slack_token: ${{ secrets.SLACK_BOT_TOKEN }}
          channel_id: ${{ secrets.SLACK_CHANNEL_ID }}
          message: "🚀 Deployment started for PR #${{ github.event.pull_request.number }}"

